
# Python
__pycache__/
*.pyc

# The published CV is built fresh on every deploy (deploy.yml) from cv/ and the
# shared publications.json, then placed here. Not committed — it's a build output.
pdfs/CV.pdf

# Generated at build time from _data/talks.yml (scripts/slides.py)
slides/index.md

# Generated at build time from _data/minicourses.yml (scripts/minicourses_page.py)
minicourses/index.md

# Stamped by deploy.yml with the deploy date for the footer's "Last updated".
_data/build.yml
